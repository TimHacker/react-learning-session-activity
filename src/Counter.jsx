import React, { useEffect, useState } from "react";

const Counter = (props) => {
  return (
    <div>
      <button onClick={() => {}}>+</button>
      <p>Counter score goes here</p>
      <button onClick={() => {}}>-</button>
    </div>
  );
};

export default Counter;
